前情提要：
正则表达式 是用来做什么的？
匹配，替换，查找，分割，字符串

1.正则表达式由哪几部分组成？都有什么用？
定界符
原子
元字符
模式修正符

2.原子有几部分组成分别包含什么？
普通字符
特殊字符
！打印字符
通用字符
原子表

3.元字符的组成及意义：
限定符 *+? {n} {n,} {n,m}
边界限制 ^ $ \b \B
.
|
()
\1 \2 \3

----错误处理----

错误分类
    1.语法错误
    2.运行时错误
    3.逻辑错误
    php处理错误的顺序

    设置是否显示错误报告
    在php.ini中设置
    display_errors = Off 关闭显示 On默认开启 （line 466）
    在脚本中设置错误报告
    ini_get() 获取php.ini文件中的某个配置项的值
    ini_set() 获取php.ini文件中的某个配置项的值

错误报告级别详见手册
    函数参考--影响PHP行为的扩展--预定义常量(或图 error_level.phg)

设置错误报告级别
1.修改php.ini 中的 error_reporting
2.在脚本中动态设置：error_reporting()函数

写错误日志
1.写入文件
2.写入系统日志

自定义错误处理
1.使用trigger_error() 函数代替die()
2.set_error_handler();

------PHP日期和时间------
Unix时间戳：我们存储在数据中的时间格式，一般是UNIX时间戳的形式存储的。
自从Unix纪元（格林威治时间1970年1月1日00:00:00）到当前时间的秒数

相关的时间函数
time()
mktime()
strtotime()
date()
获取时区，设置时区
getdate() 获取日期/时间信息